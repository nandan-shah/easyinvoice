import { createTheme } from '@material-ui/core/styles';

export const theme = createTheme({
  palette: {
    background: {
      primary: '#ffffff',
      secondary: '#7638FF',
    },
    primary: {
      main: '#7638FF',
    },
    secondary: {
      main: '#000',
    },
  },
});
